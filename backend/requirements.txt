fastapi==0.115.4
uvicorn[standard]==0.32.0
# Use the OS trust store (Windows / macOS keychain) so Hugging Face downloads
# work behind corporate TLS-intercepting proxies. See docs/TROUBLESHOOTING.md.
pip-system-certs==5.3 ; sys_platform == "win32"

python-dotenv==1.0.1
pydantic==2.9.2
soundfile==0.12.1
numpy>=1.26,<2.3
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
# Mock MP4 generation (bundles a static ffmpeg binary so mock mode works
# even if the host has no ffmpeg installed). The real MuseTalk pipeline
# still requires a system ffmpeg on PATH; see README.md.
imageio==2.36.0
imageio-ffmpeg==0.5.1
Pillow==11.0.0
# Kokoro TTS (CPU-friendly).
# Kokoro 0.9.4 supports Python 3.10-3.12. If install fails on your platform,
# see docs/TROUBLESHOOTING.md.
kokoro==0.9.4
# WebRTC signaling + MP4 streaming to the browser (see /api/webrtc/*).
aiortc==1.9.0
